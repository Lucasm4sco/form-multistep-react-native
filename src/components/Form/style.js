import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    form_container: {
        minHeight: 500,
        width: '100%',
        maxWidth: 380,
        backgroundColor: '#fff',
        borderRadius: 10,
        padding: 10
    }
});

export default styles;